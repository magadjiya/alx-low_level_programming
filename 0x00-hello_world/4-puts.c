#include <stdio.h>

/**
 * main - Entry point
 *
 * puts a sentence out
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	char sentence[] = "\"Programming is like building a multilingual puzzle";

	puts(sentence);

	return (0);
}
