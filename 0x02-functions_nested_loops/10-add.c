#include "main.h"

/**
 * add - function that adds two numbers together
 *
 * @f: first integer to be added
 *
 * @s: second integer to be added
 *
 * Return: 0
*/
int add(int f, int s)
{
	int sum = 0;

	if (sum == 0)
	{
		sum = f + s;
	}
	return (sum);
}
