#include "main.h"
#include <stdbool.h>

/**
 * _islower - entry point
 *
 * c - character
 *
 * Return: 1 if _islower == 'true', 0 if not
*/
int _islower(int c)
{
	if (_islower(c) == true)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
